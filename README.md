# craigslist-scraper
Finds new Craigslist posts via RSS feeds and downloads the posts. Uses server/client model where clients can scrape using their own IP or via proxy servers and then send their results back to the server.
